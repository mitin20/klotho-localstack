declare type RunBackportOptions = {
    timeoutSeconds?: number;
    showOra?: boolean;
    waitForString?: string;
    cwd?: string;
    keepAlive?: boolean;
};
export declare function runBackportViaCli(backportArgs: string[], runBackportOptions?: RunBackportOptions): Promise<{
    output: string;
    code: number | null;
    keypress: (keyCode: "up" | "down" | "enter", runBackportOptions?: RunBackportOptions | undefined) => Promise<{
        output: string;
    }>;
}>;
export {};

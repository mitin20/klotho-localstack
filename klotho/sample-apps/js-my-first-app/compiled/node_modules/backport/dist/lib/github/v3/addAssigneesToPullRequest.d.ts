import { ValidConfigOptions } from '../../../options/options';
export declare function addAssigneesToPullRequest({ githubApiBaseUrlV3, repoName, repoOwner, accessToken, autoAssign, interactive, }: ValidConfigOptions, pullNumber: number, assignees: string[]): Promise<void>;

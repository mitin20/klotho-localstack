import { ValidConfigOptions } from '../../../options/options';
export declare function addLabelsToPullRequest({ githubApiBaseUrlV3, repoName, repoOwner, accessToken, interactive, }: ValidConfigOptions, pullNumber: number, labels: string[]): Promise<void>;

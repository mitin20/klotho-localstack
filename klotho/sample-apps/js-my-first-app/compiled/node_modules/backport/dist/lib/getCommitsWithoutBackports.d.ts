import { Commit } from '../entrypoint.module';
import { ValidConfigOptions } from '../options/options';
export declare function getCommitsWithoutBackports({ options, commit, targetBranch, conflictingFiles, }: {
    options: ValidConfigOptions;
    commit: Commit;
    targetBranch: string;
    conflictingFiles: string[];
}): Promise<{
    formatted: string;
    commit: Commit;
}[]>;

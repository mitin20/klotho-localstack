"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const backportRun_1 = require("./backportRun");
const cliArgs_1 = require("./options/cliArgs");
const processArgs = process.argv.slice(2);
// this is the entrypoint when running from command line
(0, backportRun_1.backportRun)({ processArgs, exitCodeOnFailure: true }).then((backportResponse) => {
    const { interactive, ls } = (0, cliArgs_1.getRuntimeArguments)(processArgs);
    if (!interactive || ls) {
        // eslint-disable-next-line no-console
        console.log(JSON.stringify(backportResponse));
    }
});

import { TargetBranchChoice } from '../options/ConfigOptions';
import { Commit } from './sourceCommit/parseSourceCommit';
export declare function getChoicesForCommitPrompt(commits: Commit[], showDetails: boolean): {
    name: string;
    short: string;
    value: Commit;
}[];
export declare function promptForCommits({ commitChoices, isMultipleChoice, showDetails, }: {
    commitChoices: Commit[];
    isMultipleChoice: boolean;
    showDetails: boolean;
}): Promise<Commit[]>;
export declare function promptForTargetBranches({ targetBranchChoices, isMultipleChoice, }: {
    targetBranchChoices: TargetBranchChoice[];
    isMultipleChoice: boolean;
}): Promise<string[]>;
export declare function confirmPrompt(message: string): Promise<boolean>;

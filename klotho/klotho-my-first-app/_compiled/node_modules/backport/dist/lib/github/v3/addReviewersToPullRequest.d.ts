import { ValidConfigOptions } from '../../../options/options';
export declare function addReviewersToPullRequest({ githubApiBaseUrlV3, repoName, repoOwner, accessToken, interactive, }: ValidConfigOptions, pullNumber: number, reviewers: string[]): Promise<void>;

export interface AuthorIdResponse {
    user: {
        id: string;
    };
}
export declare function fetchAuthorId({ accessToken, author, githubApiBaseUrlV4, }: {
    accessToken: string;
    author: string | null;
    githubApiBaseUrlV4?: string;
}): Promise<string | null>;

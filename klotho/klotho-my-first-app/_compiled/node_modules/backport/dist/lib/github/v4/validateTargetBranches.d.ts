import { ValidConfigOptions } from '../../../options/options';
export interface TargetBranchResponse {
    repository: {
        ref: {
            id: string;
        } | null;
    };
}
export declare function validateTargetBranches({ accessToken, repoName, repoOwner, githubApiBaseUrlV4 }: ValidConfigOptions, targetBranches: string[]): Promise<void>;

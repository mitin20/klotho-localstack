"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.mockConfigFiles = void 0;
const promises_1 = __importDefault(require("fs/promises"));
function mockConfigFiles({ projectConfig, globalConfig, }) {
    jest
        .spyOn(promises_1.default, 'readFile')
        //@ts-expect-error
        .mockImplementation(async (filepath) => {
        if (filepath === '/path/to/project/config') {
            return JSON.stringify(projectConfig);
        }
        if (filepath.endsWith('.backport/config.json')) {
            return JSON.stringify(globalConfig);
        }
    });
}
exports.mockConfigFiles = mockConfigFiles;
